def bhaskara(a,b,c): (
    import math(sqrt)
    delta = b**2-4*a*c
    return (-b+sqrt(delta))/2*a
)


iread = input :: int

a = 'Value for A: ' |> iread
b = 'Value for B: ' |> iread
c = 'Value for C: ' |> iread

bhaskara(a,b,c) |> print

