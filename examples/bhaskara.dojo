import math(sqrt)

bhaskara = @a,b,c: (
    delta = b**2-4*a*c
    return (-b+sqrt(delta))/2*a
)

iread = read => int

a = 'Value for A: ' |> iread
b = 'Value for B: ' |> iread
c = 'Value for C: ' |> iread

write(bhaskara(a,b,c))

